server:
  port: 8082

spring:
  application:
    name: place-service

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: never
