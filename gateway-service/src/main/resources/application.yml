server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      # 라우트 규칙은 각 서비스 개발 시점에 추가합니다.
      routes: []

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: never
