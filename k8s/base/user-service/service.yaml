apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: moamap
spec:
  # 내부 전용 (게이트웨이를 통해서만 접근)
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - port: 8081
      targetPort: 8081
