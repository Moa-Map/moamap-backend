plugins {
	id 'org.springframework.boot'
}

dependencies {
	implementation project(':common')

	implementation 'org.springframework.boot:spring-boot-starter-web'
	implementation 'org.springframework.boot:spring-boot-starter-validation'
	implementation 'org.springframework.boot:spring-boot-starter-actuator'
	implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
	implementation 'org.springframework.boot:spring-boot-starter-data-redis'

	// Swagger UI
	implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.7.0'

	// JWT (jjwt)
	implementation 'io.jsonwebtoken:jjwt-api:0.12.6'
	runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.12.6'
	runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.12.6'

	// @ConfigurationProperties 메타데이터 생성
	annotationProcessor 'org.springframework.boot:spring-boot-configuration-processor'

	// ObjectStorageConfig가 S3Presigner 빈을 직접 구성하므로(AWS SDK 클래스 직접 참조),
	// common의 런타임 전이만으로는 컴파일이 되지 않아 명시적으로 선언한다(map-service/place-service와 동일).
	implementation platform('software.amazon.awssdk:bom:2.49.0')
	implementation 'software.amazon.awssdk:s3'

	runtimeOnly 'org.postgresql:postgresql'

	testRuntimeOnly 'com.h2database:h2'
}
